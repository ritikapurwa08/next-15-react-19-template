import UserButton from "@/components/features/auth/components/user-button";
import React from "react";

const page = () => {
  return (
    <div>
      <UserButton />
    </div>
  );
};

export default page;
